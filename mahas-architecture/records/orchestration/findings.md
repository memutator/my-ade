# 결함 대장 — 검증·검토에서 IMP owner로 되돌릴 finding

각 finding은 delivery.md 규칙대로 원래 IMP owner의 수정 revision으로 되돌린다.
`status`: open / fix-claimed / fixed-in-<sha> / wontfix / spec-issue.

| id | 발견 | 위치 | owner | status | evidence |
|---|---|---|---|---|---|
| F-001 | **mahasd transport 인증 부재 — 어떤 credential이든(없는 것 포함) `operator-local`이 되어 전체 92-op wildcard 접근.** forged worker credential로 operator-only `access.grant`이 실제 커밋됨. `principalId`는 검증 없는 client claim. v1 local trust model 코멘트와는 별개로, worker credential·bootstrap 경로(`bootstrap-credential.ts` 256-bit secret 발급, `rpc/worker-auth`, `mahasd-worker.sock` 엔드포인트 정의)가 전부 구현됐으나 `serveRpc`가 operator 소켓에 한 번만 바인드돼 전부 dead code — worker spawn 모델상 same-uid agent가 operator로 승격 가능. | `packages/mahas-runtime/src/main.ts:353-367`, `rpc/endpoints.ts:23`, `launch/bootstrap-credential.ts`, `composition.ts:416` (worker connection files → operator socket) | IMP-12 (transport/authz wiring), IMP-20 (bootstrap credential), IMP-23 (daemon wiring) | open | `orchestration/cli-surface.md` + `/tmp/mahas-ver-cli/` raw receipts (`cac1bc64…` forged grant commit) |
| F-002 | `surface.describe`/`operation.get`는 모든 surface에 *리스트*되지만 grant 없는 member가 호출하면 `UNAVAILABLE_OPERATION`이 아니라 `SCOPE_DENIED` — 2-name oracle + list-vs-invoke 불일치 (비노출 정책의 미세 leak). | `api/registry.ts` surface projection + admission | IMP-11 | open | `orchestration/cli-surface.md` F8 |
| F-003 | **VER-02 DEFECT-1 — restore 성공 보고하지만 external blob 경로가 틀려 content에 도달 불가.** `restoreContent`가 external blob을 `<root>/<digest>` flat으로 쓰는데 `external_ref`는 sharded `<digest[0:2]>/<digest>`를 기대 → `getContentBlob` ENOENT. | `operations/restore.ts` ~L378-402 | IMP-29 | open | `verification/VER-02.md` step 5, `evidence/VER-02/` |
| F-004 | **VER-02 DEFECT-2 — GC orphan 수집이 shard 구조를 못 읽음.** `planOrphanBlobFiles`의 비재귀 `readdirSync`가 shard *디렉토리*를 후보로 열거 → EISDIR unlink 실패 → 영구 retryable residue + `effect_intents` churn; 진짜 orphan 파일(`<shard>/<digest>`)은 영원히 미수집. | `operations/gc.ts` ~L386-425 | IMP-29 | open | `verification/VER-02.md` step 4-5 |
| F-005 | **`runtime.reconcile` RPC가 항상 `ERR_SQLITE_ERROR`로 거부.** op spec이 `mutation:false`라 admission이 `BEGIN DEFERRED`로 감싸는데 reconcile 자체 `withTx`→`BEGIN IMMEDIATE`가 같은 connection에서 중첩 (txDepth가 admission의 raw BEGIN을 못 봄). startup/restart reconcile은 무관. | `api/admission.ts:254`, lifecycle reconcile 등록 (mutation 플래그) | IMP-11 또는 IMP-23 (registration) | open | `orchestration/ver-infra.md` verified failure modes |
| F-006 | **mahasd가 stale host endpoint 파일에서 크래시.** 죽은 host + 남은 `execution-host.sock.endpoint.json` → `helloHost` dial ECONNREFUSED가 readline-Interface `error` 이벤트로 escape (`rl.on('error')` 없음) → `host-attach-failed` 로그 전에 프로세스 사망, 100% 재현. workaround: stale 파일 삭제 → clean `host-absent` degraded start. | `hostClient.ts:146` | IMP-17 | open | `orchestration/ver-infra.md` |
| F-007 | **`host.process.spawn`의 `spec.pty` 경로 FK 위반.** `host_terminals` INSERT(FK→`host_processes`)가 `persistProcess`보다 먼저 실행 → `FOREIGN KEY constraint failed` → receipt `unknown`, terminal row 없음. pty 자체는 spawn됨 (pts 확인). non-pty spawn은 정상. | `mahas-execution-host/src/process-manager.ts:373-391` | IMP-18 | open | `orchestration/ver-infra.md` |
| F-008 | (minor) 거부된 boot도 5-in-120s `CRASH_LOOP` throttle 카운트에 포함 — 반복 검증 시 인위적 차단 가능. | `main.ts` crash-loop | IMP-23 | open | `orchestration/ver-infra.md` |
