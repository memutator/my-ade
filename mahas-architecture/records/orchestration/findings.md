# 결함 대장 — 검증·검토에서 IMP owner로 되돌릴 finding

각 finding은 delivery.md 규칙대로 원래 IMP owner의 수정 revision으로 되돌린다.
`status`: open / fix-claimed / fixed-in-<sha> / wontfix / spec-issue.

| id | 발견 | 위치 | owner | status | evidence |
|---|---|---|---|---|---|
| F-001 | **mahasd transport 인증 부재 — 어떤 credential이든(없는 것 포함) `operator-local`이 되어 전체 92-op wildcard 접근.** forged worker credential로 operator-only `access.grant`이 실제 커밋됨. `principalId`는 검증 없는 client claim. v1 local trust model 코멘트와는 별개로, worker credential·bootstrap 경로(`bootstrap-credential.ts` 256-bit secret 발급, `rpc/worker-auth`, `mahasd-worker.sock` 엔드포인트 정의)가 전부 구현됐으나 `serveRpc`가 operator 소켓에 한 번만 바인드돼 전부 dead code — worker spawn 모델상 same-uid agent가 operator로 승격 가능. | `packages/mahas-runtime/src/main.ts:353-367`, `rpc/endpoints.ts:23`, `launch/bootstrap-credential.ts`, `composition.ts:416` (worker connection files → operator socket) | IMP-12 (transport/authz wiring), IMP-20 (bootstrap credential), IMP-23 (daemon wiring) | fixed-in-wip | `orchestration/cli-surface.md` + `/tmp/mahas-ver-cli/` raw receipts (`cac1bc64…` forged grant commit) |
| F-002 | `surface.describe`/`operation.get`는 모든 surface에 *리스트*되지만 grant 없는 member가 호출하면 `UNAVAILABLE_OPERATION`이 아니라 `SCOPE_DENIED` — 2-name oracle + list-vs-invoke 불일치 (비노출 정책의 미세 leak). | `api/registry.ts` surface projection + admission | IMP-11 | open | `orchestration/cli-surface.md` F8 |
| F-003 | **VER-02 DEFECT-1 — restore 성공 보고하지만 external blob 경로가 틀려 content에 도달 불가.** `restoreContent`가 external blob을 `<root>/<digest>` flat으로 쓰는데 `external_ref`는 sharded `<digest[0:2]>/<digest>`를 기대 → `getContentBlob` ENOENT. | `operations/restore.ts` ~L378-402 | IMP-29 | fixed-in-wip | `verification/VER-02.md` step 5, `evidence/VER-02/` |
| F-004 | **VER-02 DEFECT-2 — GC orphan 수집이 shard 구조를 못 읽음.** `planOrphanBlobFiles`의 비재귀 `readdirSync`가 shard *디렉토리*를 후보로 열거 → EISDIR unlink 실패 → 영구 retryable residue + `effect_intents` churn; 진짜 orphan 파일(`<shard>/<digest>`)은 영원히 미수집. | `operations/gc.ts` ~L386-425 | IMP-29 | fixed-in-wip | `verification/VER-02.md` step 4-5 |
| F-005 | **`runtime.reconcile` RPC가 항상 `ERR_SQLITE_ERROR`로 거부.** op spec이 `mutation:false`라 admission이 `BEGIN DEFERRED`로 감싸는데 reconcile 자체 `withTx`→`BEGIN IMMEDIATE`가 같은 connection에서 중첩 (txDepth가 admission의 raw BEGIN을 못 봄). startup/restart reconcile은 무관. | `api/admission.ts:254`, lifecycle reconcile 등록 (mutation 플래그) | IMP-11 또는 IMP-23 (registration) | fix-in-wip-partial | `orchestration/ver-infra.md` verified failure modes |
| F-006 | **mahasd가 stale host endpoint 파일에서 크래시.** 죽은 host + 남은 `execution-host.sock.endpoint.json` → `helloHost` dial ECONNREFUSED가 readline-Interface `error` 이벤트로 escape (`rl.on('error')` 없음) → `host-attach-failed` 로그 전에 프로세스 사망, 100% 재현. workaround: stale 파일 삭제 → clean `host-absent` degraded start. | `hostClient.ts:146` | IMP-17 | open | `orchestration/ver-infra.md` |
| F-007 | **`host.process.spawn`의 `spec.pty` 경로 FK 위반.** `host_terminals` INSERT(FK→`host_processes`)가 `persistProcess`보다 먼저 실행 → `FOREIGN KEY constraint failed` → receipt `unknown`, terminal row 없음. pty 자체는 spawn됨 (pts 확인). non-pty spawn은 정상. | `mahas-execution-host/src/process-manager.ts:373-391` | IMP-18 | open | `orchestration/ver-infra.md` |
| F-008 | (minor) 거부된 boot도 5-in-120s `CRASH_LOOP` throttle 카운트에 포함 — 반복 검증 시 인위적 차단 가능. | `main.ts` crash-loop | IMP-23 | open | `orchestration/ver-infra.md` |
| F-009 | **VER-01 DEFECT-1 (치명) — provisioning allowlist 무력화.** `checkProvisioning`이 flat `scope.allowedRoleIds` 등을 읽는데 `access.grant`는 nested `scope.provisioning.*`을 요구·저장 → 정식 발급 grant의 allowlist/maxMembers/placement가 vacuous → 허용 role 아닌 역할로 `team.assign`이 커밋됨 (re-auth under `allowedRoleIds:['r-lead']` 증명). | `coordination/member.ts:331-437` vs `access/grant.ts:291-292` | IMP-13 + IMP-10 | fixed-in-wip | `verification/VER-01.md` s4, evidence/VER-01 |
| F-010 | **VER-01 DEFECT-2 — member grant의 scope가 자기 op을 못 커버.** Assignment grant scope는 `{runId,roleId,boundaryId,taskIds}`인데 `scopeEntries`는 `runId`만 매핑 → coordinator의 `responsibility.search`가 자기 31-action 목록에 있어도 `SCOPE_DENIED` (project-scoped provisioning grant는 정상 경로). | `coordination/member.ts:574-581`, `access/grant.ts:215-224` | IMP-13 | fixed-in-wip | `verification/VER-01.md` s4/s7 |
| F-011 | **VER-01 DEFECT-3 — `locate`가 비조상 중첩을 조용히 resolve.** ad-hoc `resolveDeepest` 사용, canonical `resolveTerritory`(`model/territory.ts`) 미사용 → `src/api/auth/*`가 비조상 `b-api-alt` 중첩에도 `b-api-auth`로 resolve. ambiguous 은폐. (WIP 트리가 이미 `resolveTerritory`로 리와이어 중 — owner 동의) | `discovery/locate.ts:63-92` | IMP-06 | fixed-in-wip | `verification/VER-01.md` s3 |
| F-012 | **VER-01 DEFECT-4 — cyclic reparent가 op을 크래시.** `descendantsOf` BFS에 visited set 없음 → `RangeError: Invalid array length`이 `registry.dispatch`를 빠져나감 — rejected receipt도 `CONTAINS_CYCLE` 보고도 없음. | `model/change-set.ts:1037-1044` | IMP-04 | open | `verification/VER-01.md` s5 |
| F-013 | **VER-01 DEFECT-5 — `assignment.show`가 모든 member에게 grant되지만 stub.** 등록만 되고 handler 없음 → 모든 caller에 `UNAVAILABLE_OPERATION`. | `coordination/index.ts:76-84` | IMP-13/IMP-20 | fixed-in-wip | `verification/VER-01.md` s6 |
| F-014 | **(치명) 실행 재부착 경로 전체가 dead code.** `lifecycle/reconcile.ts:198-208`이 credential 없이 `host.hello` 전송 → `UNAUTHENTICATED`(`host.ts:511`) → 에러 삼켜짐(`reconcile.ts:276`, probe.error 분기 없음) → inventory 미취득 → 모든 execution이 `left-unknown`. orphan sweep(`recovery/reconciler.ts`)도 미배선. kill-9 후 재부팅해도 process는 live인데 control plane이 모름. VER-06/07 헤드라인 finding. (fix tree는 `reconcile.ts:233-244`에서 부분 패치 중이나 여전히 credential 미제시) | `lifecycle/reconcile.ts`, `execution-host/src/host.ts:511`, `recovery/reconciler.ts` | IMP-22 (재부착), IMP-17 (host auth) | fix-in-wip-partial | `orchestration/ver-drill.md` D-01 |
| F-015 | **cross-config-dir same-DB split-brain.** `mahasd.lock`은 config-dir 스코프 — 같은 DB를 다른 config dir로 연 두 mahasd가 모두 serve (live epoch-9가 `interrupted`로 찍히고도 계속). 단일 writer 불변식 위반. host lease는 takeover를 올바르게 거부. | `main.ts` lock scope, `reconcile.ts:684` | IMP-23 | open | `orchestration/ver-drill.md` D-04 |
| F-016 | (minor) `String(err)` fallback이 plain-object MahasError를 `[object Object]`로 렌더 — 운영 로그 가독성. | `lifecycle/reconcile.ts:242/270/276` | IMP-23 | fixed-in-wip | `orchestration/ver-drill.md` D-05 |
| F-017 | **fresh control DB가 host lease를 영원히 못 되찾음.** epoch < stored lease epoch이면 `STALE_EXECUTION` (시연: epoch 3 vs lease 14) — compose-time `host-attach-failed`에도 reconcile decision 없이 silent `ready`. DB 재생성/복구 시나리오 차단. | `reconcile.ts` host acquire + epoch 비교 | IMP-17/IMP-22 | fix-in-wip-partial | `orchestration/ver-drill.md` D-06 |
| F-018 | **VER-03 (치명) — member↔principal binding 미검사.** worker principalId + reviewer memberId를 얹은 ctx가 reviewer의 delivery를 `delivery.ack` 커밋 (`status=acknowledged` 지속). `decide()`는 principalId로 grant만 조회하고 `ctx.memberId`가 그 principal 소유인지 검증 안 함 — 임의 memberId를 임의 principal 권한에 결합 가능. | `access/authorize.ts` decide(), `admission.ts` ctx 조립 | IMP-10 | open | `verification/VER-03.md` s3, `/tmp/mahas-ver-03/out/s3-bypass.json` |
| F-019 | **VER-03 — 자기 grant 자진 폐기가 항상 rollback.** spec은 subject의 self-revocation 허용. `accessRevokeOp`가 revoke까지 수행하지만 admission post-handler 재인가가 방금 폐기된 attested grant를 재검사 → `GRANT_REVOKED` → 전체 txn rollback (`revoked_at` NULL 유지). post-check이 "이 op이 방금 폐기한 grant"를 exempt하지 않음. | `api/admission.ts:311-314`, `access/operations.ts:322-346` | IMP-10/IMP-11 | open | `verification/VER-03.md` s4 |
| F-020 | **VER-03 — `access.inspect` self-view 도달 불가.** contract은 subject의 자기 binding 조회용인데 member grant action set에 미포함 → self/own-grant 조회 모두 `UNAVAILABLE_OPERATION` (handler self branch까지 도달 못함). | member grant action sets, `access/operations.ts` | IMP-11/IMP-13 | open | `verification/VER-03.md` s5 |
| F-021 | **VER-03 — member grant scope가 boundary 타겟을 영원히 커버 불가.** `scopeEntries()`는 assignment scope를 `[run]`으로만 매핑 (flat `boundaryId`/`taskIds` 무시). boundary의 실제 조상은 `modelVersion→project` — run 미포함 → 자기 boundary조차 `responsibility.inspect` SCOPE_DENIED. action set엔 있는데 도달 불가 = 기록된 narrowing이 양방향 모두 dead. (F-010과 동일 근본 원인, 별도 관측점) | `access/grant.ts:237-260` scopeEntries, `actual-targets.ts` | IMP-10/IMP-13 | fixed-in-wip | `verification/VER-03.md` s6 |
| F-022 | **VER-03 (치명) — 외부 grant attestation 확대.** `callerGrantsOfKind`/`recheckCallerGrants`가 `ctx.grantRevisions` 항목을 grant id로만 조회, `principal_id` vs `ctx.principalId` 비교 없음. lead ctx가 operator-local의 `provAll`을 attest해 r-doc member 2개 커밋 (`mem_9af55cf9`, `mem_c5c5da41`, `parentProvisioningGrant=provAll`로 기록). grant id만 알면 누구든 타인 grant를 자기 attestation으로 차용. | `coordination/internal.ts:494-530` | IMP-10 | fix-in-wip-partial | `verification/VER-03.md` s6 |
| F-023 | **VER-03 — `execution_credentials.revoked_at` 미참조 + worker endpoint 미바인드.** `mahasd-worker.sock`이 `main.ts`에서 한 번도 바인드되지 않음 (`mahasdWorkerEndpoint` 정의만 존재) → worker-auth 경로 전체 dead code. credential row를 revoke해도 member grant로 ordinary op이 계속 커밋 — credential 테이블은 쓰이기만 하고 op 경로에서 읽히지 않음. (F-001 확장) | `main.ts:368-378`, `rpc/endpoints.ts:22`, `rpc/worker-auth.ts` | IMP-12/IMP-19/IMP-20 | fixed-in-wip | `verification/VER-03.md` s3/환경 |
| F-024 | **VER-03 — admission이 grant revision 일치를 검사 안 함.** `decide()`는 attested grant의 존재+미폐기만 확인 (revision 비교 없음) → stale revision attestation이 admission 통과, mutation 경계의 `recheckCallerGrants`에서만 fence. read op은 영원히 미fence. | `access/authorize.ts` grantRevisions 검사 vs `internal.ts:498-504` | IMP-10 | open | `verification/VER-03.md` s3/s4 |
| F-025 | **client-probe — `subscriptionId`는 dead handle.** `pollSubscription`/`SubscriptionHub.closeSession`이 dead code (호출자 0), unsubscribe/poll op 부재 → 매 `runtime.subscribe` 호출이 orphan hub entry를 남김. pull 모델 자체는 정상 (snapshot→cursor→누락 이벤트 재전달 확인). | subscribe hub 등록 경로 | IMP-21 | open | `orchestration/client-probe.md` |
| F-026 | **client-probe (치명 후보) — wire에서 event visibility 필터가 사실상 무력.** authenticator가 `ctx.memberId`를 스탬프 안 함 → `visibilityRuleFor`가 항상 `unrestricted` → `eventVisible`/`runtime.snapshot`의 member-redaction 분기가 wire상 dead code — subscribe를 커버하는 grant만 있으면 operator급 이벤트 스트림을 읽음. (F-001 파생 — 인증이 identity를 도출 안 하니 visibility 계층이 받을 입력이 없음) | `main.ts:353-367`, visibility/eventVisible 경로 | IMP-12 + IMP-10 | open | `orchestration/client-probe.md` |
| F-027 | **client-probe — `worker.prepare`가 `INVALID_TRANSITION`으로 도달 불가.** `ensureEnvelope`가 snake_case `AssignmentRow`에서 camelCase `taskId`를 읽어 항상 undefined로 판정 (probable bug). coordination assignment도 `context.build` snapshot pin + executable-less profile로 실패 → `client.view.bind`의 committed 경로가 이 revision에서 생성 불가. | `composition.ts:395-402` | IMP-19/IMP-20 | open | `orchestration/client-probe.md` blocked section |
| F-028 | **client-probe — malformed payload가 bare `TypeError`로 admission 탈출.** client는 `status:'unknown'` + `CONTROL_UNAVAILABLE`, receipt 미영속 → 진짜 ambiguous dispatch 실패와 구분 불가 (receipt 정직성 위반). | admission/ops payload validation 경계 | IMP-11 | open | `orchestration/client-probe.md` |
| F-029 | **VER-04 (치명) — `task.dispatch`가 정산 안 된/거부된 output을 입력으로 소비.** task-output binding을 `latestOutcome`으로 resolve하는데 settlement 검사 없음 (`eligibility.ts:223-234`) → **rejected** output(t-impl-c@3)과 **unsettled rev2**(t-join)를 그대로 소비. 같은 projection은 그 edge를 `decision=none`으로 차단 — 두 resolver가 불일치. strict `pinInputs`는 정상. | `member.ts:970`→`eligibility.ts:223-234` | IMP-13/IMP-21 | open | `verification/VER-04.md` s1 |
| F-030 | **VER-04 — canonical `InputBinding` 키를 resolver가 못 읽음.** spec 정본 top-level `taskId`/`outputSlot`은 무시되고 `identity.*` alias만 동작 → canonical 입력은 영구 `INPUT_NOT_READY`. | `eligibility.ts:173,211-213,237` | IMP-13 | open | `verification/VER-04.md` s1 |
| F-031 | **VER-04 — caller-supplied `inputBindings` pin이 선언 provenance를 우회.** wrong-producer artifact가 task-output slot에 수용됨; artifact lookup에 `run_id` 필터도 없어 cross-run 참조 가능. | `member.ts:978-1005` | IMP-13 | open | `verification/VER-04.md` s1 |
| F-032 | **VER-04 — assignment `taskIds` narrowing이 dispatch에서 미적용.** `t-impl-b`만 배정된 member에게 `t-consult-b`를 dispatch 시도 — taskIds fence 없음 (role fence가 `INVALID_TRANSITION`로 대신 막아 우연히 차단됨). | `member.ts:952-956` | IMP-13 | open | `verification/VER-04.md` s1 |
| F-033 | **VER-10-prep (delivery 무결성) — `.gitignore`가 IMP-25 codex profile 전체를 무시.** `.gitignore`의 `codex` 패턴이 `packages/mahas-harness-config/src/codex/`를 통째로 가려서 3개 파일(1,064줄)이 untracked WIP로만 존재 — **한 번도 커밋되지 않음**. `index.ts:137-139`가 export하는데 pinned `83a6d21`+HEAD 모두 `tsc` TS2307 ×3 실패. `launch/recipe-adapter.ts`도 untracked. STATUS의 "IMP-25 landed"는 export만 참 — VER-10이 검증 가능한 clean revision이 존재하지 않음. | `.gitignore` line 7, `packages/mahas-harness-config/src/codex/`, `index.ts:137-139` | IMP-25 | open | `orchestration/ver10-prep.md` B1 |
| F-034 | **VER-08 — `workspace.prepare`의 host effect intent가 영구 미배송.** op은 durable claim+checkout을 커밋하지만 `host.workspace.prepare` effect intent가 host에 도달 안 함 — `drainEffectOutbox`가 `process.stop` 외 kind를 스킵하고 호출자도 없음 → 물리 checkout 미생성 + claim이 canonical path를 `RESOURCE_BUSY`로 영구 wedge. | drainEffectOutbox (host effect delivery 경로) | IMP-16/IMP-17 | open | `verification/VER-08.md` s4 |
| F-035 | **VER-08 — lease `expires_at`의 적용이 경로마다 다름.** `host_controller_lease.expires_at`은 기록되는데 `requireLeaseProof`는 무시 (죽은 controller의 잔여 fence로 spawn 성공), `assertLease`는 강제 — expiry semantics 불일치. | `lease.ts` requireLeaseProof vs assertLease | IMP-17/IMP-22 | open | `verification/VER-08.md` s3 |
| F-036 | **VER-08 — socket frame 파이프라인 비직렬화.** `void onLine`로 frame이 직렬 처리 안 돼 pipelined call이 `hello` auth와 경주 — timing에 따라 `unknown/CONTROL_UNAVAILABLE` vs `committed` (fail-safe이나 비결정적). | `rpc/local-server.ts` onLine 처리 | IMP-12 | open | `verification/VER-08.md` s6 |
| F-037 | **VER-08 — `drain-and-stop`이 worker stop 0건 발행하고 `completed` 기록.** "no cross-domain caller wired (worker.stop is IMP-22)"로 residual은 정직하게 남기나 실제 drain은 불가 — stop-intents: completed 표기와 실제 효과 불일치. | drain-and-stop handler (IMP-22 미배선) | IMP-22 | open | `verification/VER-08.md` s2 |
| F-038 | **VER-07 — host gap/replay 스키마와 mahasd `extractGap` 불일치.** host는 `gap{droppedThrough}`+replay 배열을 내는데 mahasd는 다른 스키마를 기대 → 매 attach가 `gap:null, replayFrom:null` → client가 출력 손실을 감지 불가. | host attach 응답 ↔ mahasd `extractGap` | IMP-17/IMP-18 | open | `verification/VER-07.md` p4/p5 |
| F-039 | **VER-07 — string `outputEpoch`이 `optNumber()`에서 drop → TypeError → `unknown/CONTROL_UNAVAILABLE`.** epoch를 string으로 보낸 호출이 타입 예외로 admission 탈출 (F-028과 같은 계열). | terminal op input 파싱 (`optNumber`) | IMP-11/IMP-17 | open | `verification/VER-07.md` p4/p5 |
| F-040 | **VER-07 (인가 결함) — retarget된 binding이 subscription 소유권을 잃어 외부 principal이 타인의 live stream을 detach 커밋.** binding retarget 시 subscription ownership 미갱신 → foreign ctx의 `terminal.detach`/`view.unbind`가 다른 client의 stream을 끊음. | view bind retarget ↔ subscription 소유권 | IMP-21 | open | `verification/VER-07.md` p4 |
| F-041 | **VER-07 — `dropConnection`이 conn-close 경로에 미배선.** 죽은 connection의 subscription이 영구 orphan (hub 누적, F-025와 같은 계열). | conn-close cleanup 경로 | IMP-21 | open | `verification/VER-07.md` p6 |
| F-042 | **VER-07 — `client.view.unbind`가 `host.detach`를 호출 안 함.** unbind는 커밋되지만 host subscription이 leak — orphan stream이 계속 출력을 소비. | `client.view.unbind` handler | IMP-21 | open | `verification/VER-07.md` p3/p4 |
| F-043 | **VER-07 — escalate-stop 재검증이 grace window 내 자연사를 `PROCESS_UNVERIFIABLE`로 던짐.** row가 `stop_unknown`으로 남음 (3회 재현). 죽음이 관측됐는데 unverifiable로 보고 — 상태 정직성 미세 위반. | escalate-stop re-verify | IMP-17/IMP-18 | open | `verification/VER-07.md` p7 |
| F-044 | **VER-07 — 죽은 mahasd↔host session이 재dial 안 됨 (`localSession` corpse).** host kill 후 mahasd가 살아있어도 session 복구 없음 — reconcile이 session 수준을 못 고침 (F-014의 session-layer 확인). | host session 재연결 경로 | IMP-17/IMP-22 | open | `verification/VER-07.md` p7b |
| F-045 | **VER-07 — host write-path가 flooded/dead consumer에서 wedge.** 932M syscw, ~16분 spin, ~80% CPU — dead consumer를 향한 쓰기가 host를 마비 (p5-hostwedge incident). | host output fanout/backpressure | IMP-17/IMP-18 | open | `verification/VER-07.md` p5-hostwedge |
| F-046 | **VER-05 (치명 경로) — coordinator의 `workspace.prepare` payload가 op contract과 불일치, 실패가 `unknown`으로 퇴화.** `stageResourcesClaimed`가 `{reservationId, placementIntent, owner, memberId, runId, purpose, mode}`를 보내는데 op은 `projectId`+`ownerReservation` 요구 (`INPUT_NOT_READY`). `OperationCallError` 래퍼가 이를 INTERNAL/unknown(reconcile)으로 표면 — validation 거부를 ambiguous로 오인. **shipped `worker.start`는 항상 `resources_claimed`에서 사망** — 83a6d21에서 spawn/attachment/join 불가. | `launch/start-coordinator.ts:739-746` | IMP-19/IMP-16 | open | `verification/VER-05.md` s3 |
| F-047 | **VER-05 — 초기 stdin이 shipped 경로에서 조용히 drop.** coordinator는 payload-level `spawnPayload.initialStdin`에 두는데 host는 `spec.initialStdin`을 소비 → child는 0 bytes 수신 (sha `e3b0c4`=empty), receipt는 route `stdin` digest `a9b31d…`로 기록 — **전달이 아니라 의도를 기록, mandatory 초기 입력의 무음 손실**. spec-field 경로는 정상 (sha `98581e…` 일치). | `start-coordinator.ts:1009-1016` vs host `pty-manager.ts:27,170-172,261-263` | IMP-19/IMP-18 | open | `verification/VER-05.md` s3, VER-06 s7 |
| F-048 | **VER-05 — `worker.start`가 worker의 post-join grant를 발급 안 함.** coordinator는 worker principal + bootstrap credential만 발행 — post-join op(`execution.join`, `task.accept`)이 worker principal에 `UNAVAILABLE_OPERATION` → agent가 launch protocol이 할당한 op을 수행 불가. | `launch/start-coordinator.ts` grant 발급 부재 | IMP-19/IMP-10 | open | `verification/VER-05.md` s3 |
| F-049 | **VER-05/VER-06 — join이 commit-time 재인가에서 rollback.** `execution.join` handler가 `worker_joins`를 tx 중간에 insert → post-handler 재인가가 이미 joined로 보고 bootstrap bypass 불적용 → worker grant 부재(F-048)와 함께 `SCOPE_DENIED` → join이 아무것도 커밋 안 함. 완벽한 join도 이 revision에선 착지 불가. | `launch/join.ts` + admission post-check | IMP-19 + IMP-10/11 | open | `verification/VER-05.md` s3, VER-06 s7 |
| F-050 | **VER-05/VER-06 — dispatch phase `assigned`가 join advancement로 도달 불가 → `task.accept` 불능.** `admitDispatch`는 phase `'assigned'`로 insert, join advancement는 `('reserved','starting','awaiting_join')`만 재기록 → committed join 후에도 dispatch가 `assigned`에 갇힘 → `awaiting_accept` 불도달 (VER-06: real spawn→join→accept로 재현, `INVALID_TRANSITION`). | `start-coordinator.ts:605` vs `join.ts:259-261` | IMP-19/IMP-14 | open | `verification/VER-05.md` s3, VER-06 s7 |
| F-051 | **VER-05 — compiler→materializer manifest 스키마 불일치.** `context.build`가 `{blobDigest, installPath, activation}`을 내는데 `parseBundleManifest`는 `{digest, path, scope, loadPhase}` 요구 → 실제 컴파일 번들이 `MODEL_INVALID 'missing digest'`로 거부 — shipped 파이프라인 자기 산출물을 못 읽음. | `realization/component-store.ts:159-167` vs context.build 출력 | IMP-08/IMP-09 | open | `verification/VER-05.md` s2 |
| F-052 | **VER-06 — 죽은 client가 execution-host daemon 전체를 죽임.** kill-first crash gap에서 host가 죽은 소켓에 spawn 응답을 쓰다가 `write EPIPE`로 프로세스 사망 (`host.ts:719`) — orphan child+journal은 생존, 재시작 후 `awaiting_join` 복구 (respawn 없음). 단일 client liveness 결함. | `mahas-execution-host/src/host.ts:719` | IMP-17 | open | `verification/VER-06.md` s5 |
| F-053 | **VER-06 — spawn `attempting` crash-gap이 `INVALID_TRANSITION`으로 영구 wedge, `START_UNKNOWN` 불도달.** attempting effect+orphan 존재 시 retry가 명세된 `START_UNKNOWN` 모호성 경로가 아닌 INVALID_TRANSITION → exec `preparing`+effect `attempting` 영구 정체 (respawn은 안전하게 차단되나 복구 불가). | `start-coordinator.ts` attempting→unknown 정규화 | IMP-19 | open | `verification/VER-06.md` s5 |
| F-054 | **VER-06 — host spawn dedupe가 `effectKey` 단일키.** 같은 exec+nonce+다른 key → 두 번째 child spawn + `host_processes` row 덮어쓰기 → 첫 child는 live이나 row로 주소 불가 (uncontrollable orphan). control plane의 plan-stable effectKey가 shipped 경로를 우연히 보호하지만 host 경계 자체는 중복 억제 없음. | `host.process.spawn` dedupe 키 | IMP-17/IMP-18 | open | `verification/VER-06.md` s1 |
| F-055 | **VER-06 — member grant가 `execution.join` 타겟 세트를 구조상 커버 불가.** `joinResolveTargets`가 `{execution, member, launchPlan}`을 내는데 `expandOne`에 `launchPlan` 조상이 없어 정확한 scope 엔트리나 `*`만 커버 → run-scope member grant는 post-write 재인가를 절대 못 넘음 (bootstrap admission은 통과, handler 실행, 재인가 `unresolvedTargets: [launchPlan]` → 전체 rollback). F-021과 같은 형태. | `launch/join.ts:438-448` + `actual-targets.ts` | IMP-10 + IMP-19 | open | `verification/VER-06.md` s7 |
| F-056 | **VER-06 (minor) — `terminal.input`이 effect-journal 미기록.** 동일 호출이 child에 bytes를 중복 기록 (pty 실측) — receipt 정본 관점에서 replay/충돌 보호 대상 밖. borderline spec 해석. | terminal.input journal 경로 | IMP-17/IMP-18 | open | `verification/VER-06.md` notes |
| F-057 | **VER-10 (minor) — `isBootstrapOperationAllowed` dead code + 오도 주석.** 정의만 있고 호출 지점 없음; `bindingToContextFields`가 `mode`를 버림. 주석은 "transport layer에서 강제"라 주장하나 실제는 `decide()`의 execution join-state 검사(`authorize.ts:386-393`)가 동일 4-op을 강제 — 오늘은 중복이라 무해하나 주석이 보안 위치를 거짓 안내. | `launch/bootstrap-credential.ts:49-59` + main.ts/join.ts 주석 | IMP-12/IMP-19 | open | `verification/VER-10.md` O-1 |
