# ADE 0.1.3 — 반영 요구사항 점검표

버전: `0.1.3` (`dist/ade-0.1.3.AppImage`, `dist/ade_0.1.3_amd64.deb`)

## 종료 동작

- [ ] 앱을 종료하면 터미널에서 실행 중이던 에이전트 프로세스가 함께 종료됨  
  비고: 이전에는 앱 종료 후에도 쉘/에이전트가 고아 프로세스로 남았음 — 종료 후 `pgrep -f claude` 등으로 확인
- [ ] 종료 직전에 발생한 세션 상태 변경(세션 종료/등록)이 유실되지 않고 다음 실행에 반영됨  
  비고: 디바운스된 상태 저장이 종료 시 flush되도록 수정

## 세션 복구

- [ ] 같은 디렉토리·같은 pane의 여러 세션이 재시작 후 각각의 탭으로 복구 제안됨  
  비고: 0.1.2 이전에 기록된 잘못 귀속된 레코드는 이번 버전에서 일괄 폐기 — 이후 새로 잡히는 세션부터 정확히 동작
- [ ] 복구된 세션이 자신의 원래 탭에서 다시 실행됨 (다른 탭의 프롬프트로 새지 않음)  
  비고:

## 기타

- [ ] `node tools/ade-fake.mjs`를 터미널 탭에서 실행하면 가짜 에이전트가 뜨고 n/c/e/x 키로 이벤트를 발화함  
  비고: 개발용 테스트 도구 — `node tools/e2e.mjs`가 자동 시나리오 실행
